name: Build Releases
on:
  push:
    tags:
      - "v*.*.*"

jobs:
  releases:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24.1'
      - name: Build Binary
        run: |
          mkdir -p dist/
          export GOOS=linux
          export GOARCH=amd64
          output_name=sbctl-$GOOS-$GOARCH
          CGO_ENABLED=0 go build -o dist/$output_name ./

          export GOOS=windows
          output_name=sbctl-$GOOS-$GOARCH.exe
          tray_output_name=sbtray-$GOOS-$GOARCH.exe
          CGO_ENABLED=0 go build -o dist/$output_name ./
          Go111MODULE=on go build -ldflags "-H=windowsgui" -o dist/$tray_output_name ./sbtray
      - name: Sum
        run: |
          cd dist
          sha256sum ./* > checksums.txt
      - name: Release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: dist/*
